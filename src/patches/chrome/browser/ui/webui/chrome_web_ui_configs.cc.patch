diff --git a/chrome/browser/ui/webui/chrome_web_ui_configs.cc b/chrome/browser/ui/webui/chrome_web_ui_configs.cc
index 9daef52054236..d4f6fc8bad782 100644
--- a/chrome/browser/ui/webui/chrome_web_ui_configs.cc
+++ b/chrome/browser/ui/webui/chrome_web_ui_configs.cc
@@ -4,6 +4,10 @@
 
 #include "chrome/browser/ui/webui/chrome_web_ui_configs.h"
 
+#include "dao/browser/ui/webui/dao_agent_ui.h"
+#include "dao/browser/ui/webui/dao_sidebar_ui.h"
+#include "dao/browser/ui/webui/dao_welcome_ui.h"
+
 #include "build/android_buildflags.h"
 #include "build/branding_buildflags.h"
 #include "build/build_config.h"
@@ -246,6 +250,15 @@ void RegisterChromeWebUIConfigs() {
   map.AddWebUIConfig(
       std::make_unique<AccessibilityAnnotatorInternalsUIConfig>());
   map.AddWebUIConfig(std::make_unique<AutofillInternalsUIConfig>());
+
+  // Dao: Register WebUI pages
+  map.AddWebUIConfig(std::make_unique<dao::DaoAgentUIConfig>());
+  map.AddWebUIConfig(std::make_unique<dao::DaoDreamUIConfig>());
+  map.AddWebUIConfig(std::make_unique<dao::DaoIndexUIConfig>());
+  map.AddWebUIConfig(std::make_unique<dao::DaoMemoryUIConfig>());
+  map.AddWebUIConfig(std::make_unique<dao::DaoSidebarUIConfig>());
+  map.AddWebUIConfig(std::make_unique<dao::DaoSkillsUIConfig>());
+  map.AddWebUIConfig(std::make_unique<dao::DaoWelcomeUIConfig>());
   map.AddWebUIConfig(std::make_unique<BluetoothInternalsUIConfig>());
   map.AddWebUIConfig(std::make_unique<BrowsingTopicsInternalsUIConfig>());
   map.AddWebUIConfig(
@@ -483,7 +496,8 @@ void RegisterChromeWebUIConfigs() {
 
 #if BUILDFLAG(IS_WIN) || BUILDFLAG(IS_MAC) || BUILDFLAG(IS_LINUX) || \
     BUILDFLAG(IS_CHROMEOS)
-  map.AddWebUIConfig(std::make_unique<skills::SkillsUIConfig>());
+  // Dao: Replaced by DaoSkillsUIConfig above (both registered "dao://skills/").
+  // map.AddWebUIConfig(std::make_unique<skills::SkillsUIConfig>());
 #endif  // BUILDFLAG(IS_WIN) || BUILDFLAG(IS_MAC) || BUILDFLAG(IS_LINUX) || \
         // BUILDFLAG(IS_CHROMEOS)
 }
