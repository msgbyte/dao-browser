diff --git a/chrome/browser/ui/tabs/tab_strip_model.cc b/chrome/browser/ui/tabs/tab_strip_model.cc
index 21e1496f7e..93ec5a54ec 100644
--- a/chrome/browser/ui/tabs/tab_strip_model.cc
+++ b/chrome/browser/ui/tabs/tab_strip_model.cc
@@ -3446,8 +3446,9 @@ int TabStripModel::DetermineInsertionIndex(ui::PageTransition transition,
     // with the opener.
     return index + 1;
   }
-  // In other cases, such as Ctrl+T, open at the end of the strip.
-  return count();
+  // Dao: In other cases (Ctrl+T, bookmarks, etc.), insert at the top of the
+  // strip so new tabs appear at the top of the vertical tab list.
+  return 0;
 }
 
 void TabStripModel::GroupCloseStopped(const tab_groups::TabGroupId& group) {
