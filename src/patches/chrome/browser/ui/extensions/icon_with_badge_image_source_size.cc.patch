diff --git a/chrome/browser/ui/extensions/icon_with_badge_image_source.cc b/chrome/browser/ui/extensions/icon_with_badge_image_source.cc
index 84ee720..13a3218 100644
--- a/chrome/browser/ui/extensions/icon_with_badge_image_source.cc
+++ b/chrome/browser/ui/extensions/icon_with_badge_image_source.cc
@@ -106,7 +106,7 @@ void IconWithBadgeImageSource::SetBadge(std::unique_ptr<Badge> badge) {
                 badge_.get(), get_color_provider_callback_.Run()))
           : badge_->text_color;
 
-  constexpr int badge_height = 14;
+  const int badge_height = std::max(1, 14 + badge_->font_size_delta);
 
 #if BUILDFLAG(IS_ANDROID)
   // Since typography is not currently supported on Android, we use a hardcode
@@ -129,7 +129,7 @@ void IconWithBadgeImageSource::SetBadge(std::unique_ptr<Badge> badge) {
       kMaxTextWidth, gfx::Canvas::GetStringWidth(utf16_text, base_font));
   // Calculate badge size. It is clamped to a min width just because it looks
   // silly if it is too skinny.
-  constexpr int kPadding = 2;
-  int badge_width = text_width + kPadding * 2;
+  const int padding = std::max(0, 2 + badge_->font_size_delta / 2);
+  int badge_width = text_width + padding * 2;
 
   const gfx::Rect icon_area = GetIconAreaRect();
