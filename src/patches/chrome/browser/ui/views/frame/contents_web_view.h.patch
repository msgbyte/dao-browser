diff --git a/chrome/browser/ui/views/frame/contents_web_view.h b/chrome/browser/ui/views/frame/contents_web_view.h
index 3ac40b7618..4deac95426 100644
--- a/chrome/browser/ui/views/frame/contents_web_view.h
+++ b/chrome/browser/ui/views/frame/contents_web_view.h
@@ -6,10 +6,12 @@
 #define CHROME_BROWSER_UI_VIEWS_FRAME_CONTENTS_WEB_VIEW_H_
 
 #include <memory>
+#include <optional>
 
 #include "base/memory/raw_ptr.h"
 #include "chrome/browser/ui/views/frame/web_contents_close_handler_delegate.h"
 #include "chrome/common/buildflags.h"
+#include "third_party/skia/include/core/SkColor.h"
 #include "ui/base/interaction/element_identifier.h"
 #include "ui/base/metadata/metadata_header_macros.h"
 #include "ui/gfx/geometry/rounded_corners_f.h"
@@ -41,6 +43,10 @@ class ContentsWebView : public views::WebView,
   // Toggles whether the background is visible.
   void SetBackgroundVisible(bool background_visible);
 
+  // Overrides the default themed background color used for the native
+  // WebContents host and the fallback solid-color layer.
+  void SetBackgroundOverrideColor(std::optional<SkColor> color);
+
   const gfx::RoundedCornersF& GetBackgroundRadii() const;
   void SetBackgroundRadii(const gfx::RoundedCornersF& radii);
 
@@ -65,6 +71,7 @@ class ContentsWebView : public views::WebView,
   std::unique_ptr<WebContentsCloseHandler> web_contents_close_handler_;
 
   bool background_visible_ = true;
+  std::optional<SkColor> background_override_color_;
 
   std::unique_ptr<ui::LayerTreeOwner> cloned_layer_tree_;
 };
