diff --git a/chrome/browser/ui/views/frame/browser_view_layout.cc b/chrome/browser/ui/views/frame/browser_view_layout.cc
index 46f6be18ac..43f772093c 100644
--- a/chrome/browser/ui/views/frame/browser_view_layout.cc
+++ b/chrome/browser/ui/views/frame/browser_view_layout.cc
@@ -31,6 +31,17 @@
 #include "chrome/browser/ui/views/frame/immersive_mode_controller.h"
 #include "chrome/browser/ui/views/frame/tab_strip_region_view.h"
 #include "chrome/browser/ui/views/frame/top_container_view.h"
+#include "dao/browser/ui/views/dao_address_bar_view.h"
+#include "dao/browser/ui/views/dao_colors.h"
+#include "dao/browser/ui/views/dao_command_bar_view.h"
+#include "dao/browser/ui/views/dao_corner_overlay_view.h"
+#include "dao/browser/ui/views/little_dao/dao_little_dao_controller.h"
+#include "dao/browser/ui/views/little_dao/dao_little_dao_view.h"
+#include "dao/browser/ui/views/sidebar/dao_download_flyout_view.h"
+#include "dao/browser/ui/views/sidebar/dao_sidebar_view.h"
+#include "dao/browser/ui/views/dao_agent_sidebar_view.h"
+#include "dao/browser/ui/views/dao_control_center_popup.h"
+#include "ui/compositor/layer.h"
 #include "chrome/browser/ui/views/infobars/infobar_container_view.h"
 #include "chrome/browser/ui/views/side_panel/side_panel.h"
 #include "chrome/browser/ui/views/tabs/tab_strip.h"
@@ -337,6 +348,66 @@ int BrowserViewLayout::NonClientHitTest(const gfx::Point& point) {
     }
   }
 
+  // Dao: When control center popup is visible, make it clickable.
+  auto* dao_cc_popup = browser_view_->dao_control_center_popup();
+  if (dao_cc_popup && dao_cc_popup->GetVisible() &&
+      dao_cc_popup->bounds().Contains(point_in_browser_view_coords)) {
+    return HTCLIENT;
+  }
+
+  // Dao: When command bar is visible, its entire area is client (clickable).
+  // This must come first so the scrim and textfield receive mouse events.
+  auto* dao_cmd_bar = browser_view_->dao_command_bar();
+  if (dao_cmd_bar && dao_cmd_bar->GetVisible() &&
+      dao_cmd_bar->bounds().Contains(point_in_browser_view_coords)) {
+    return HTCLIENT;
+  }
+
+  // Dao: When sidebar is active, make the area above content draggable.
+  // Return HTNOWHERE so NonClientView checks window controls first,
+  // then falls back to HTCAPTION (allows window dragging).
+  // But exclude the address bar and agent sidebar so they remain clickable.
+  dao::DaoSidebarView* dao_sidebar_hit = browser_view_->dao_sidebar();
+  if (dao_sidebar_hit && dao_sidebar_hit->GetVisible()) {
+    auto* dao_addr_bar = browser_view_->dao_address_bar();
+    if (dao_addr_bar && dao_addr_bar->GetVisible() &&
+        dao_addr_bar->bounds().Contains(point_in_browser_view_coords)) {
+      return HTCLIENT;
+    }
+    auto* dao_agent = browser_view_->dao_agent_sidebar();
+    if (dao_agent && dao_agent->GetVisible() &&
+        dao_agent->bounds().Contains(point_in_browser_view_coords)) {
+      return HTCLIENT;
+    }
+    int sidebar_right = dao_sidebar_hit->bounds().right();
+    int content_top = contents_container_->y();
+    if (point_in_browser_view_coords.x() >= sidebar_right &&
+        point_in_browser_view_coords.y() < content_top) {
+      return HTNOWHERE;
+    }
+  }
+
+  // Dao: Little Dao window — traffic lights are in the header bar,
+  // only the textfield and button are clickable, everything else is draggable.
+  auto* little_dao_view = browser_view_->dao_little_dao_view();
+  if (little_dao_view && little_dao_view->GetVisible()) {
+    // The "Open in Dao" button must remain clickable
+    gfx::Rect btn_bounds = little_dao_view->open_in_dao_button_bounds();
+    if (btn_bounds.Contains(point_in_browser_view_coords)) {
+      return HTCLIENT;
+    }
+    // The URL display button must remain clickable
+    gfx::Rect display_bounds = little_dao_view->url_display_bounds();
+    if (display_bounds.Contains(point_in_browser_view_coords)) {
+      return HTCLIENT;
+    }
+    // Everything else in and above the header bar — draggable
+    int content_top = contents_container_->y();
+    if (point_in_browser_view_coords.y() < content_top) {
+      return HTNOWHERE;
+    }
+  }
+
   // Determine if the TabStrip exists and is capable of being clicked on. We
   // might be a popup window without a TabStrip.
   if (delegate_->ShouldDrawTabStrip()) {
@@ -638,6 +709,21 @@ int BrowserViewLayout::LayoutToolbar(int top) {
   int browser_view_width = vertical_layout_rect_.width();
   bool toolbar_visible = delegate_->IsToolbarVisible();
   int height = toolbar_visible ? toolbar_->GetPreferredSize().height() : 0;
+
+  // Dao: When sidebar or Little Dao is active, move toolbar off-screen
+  dao::DaoSidebarView* dao_sidebar_for_toolbar = browser_view_->dao_sidebar();
+  bool dao_has_sidebar = dao_sidebar_for_toolbar &&
+                         dao_sidebar_for_toolbar->GetVisible();
+  bool dao_is_little_dao = dao::DaoLittleDaoController::IsLittleDaoWindow(
+      browser_view_->browser());
+  if ((dao_has_sidebar || dao_is_little_dao) &&
+      toolbar_visible && height > 0) {
+    toolbar_->SetVisible(true);
+    toolbar_->SetBounds(vertical_layout_rect_.x(), -height,
+                        browser_view_width, height);
+    return top;
+  }
+
   SetViewVisibility(toolbar_, toolbar_visible);
   toolbar_->SetBounds(vertical_layout_rect_.x(), top, browser_view_width,
                       height);
@@ -648,32 +734,14 @@ int BrowserViewLayout::LayoutBookmarkAndInfoBars(int top, int browser_view_y) {
   TRACE_EVENT0("ui", "BrowserViewLayout::LayoutBookmarkAndInfoBars");
   dialog_top_y_ = top + browser_view_y - kConstrainedWindowOverlap;
 
+  // Dao: Always skip bookmark bar and separator
   if (bookmark_bar_) {
-    top = std::max(toolbar_->bounds().bottom(), LayoutBookmarkBar(top));
-  }
-
-  if (delegate_->IsContentsSeparatorEnabled() &&
-      (toolbar_->GetVisible() || bookmark_bar_) && top > 0) {
-    SetViewVisibility(contents_separator_, true);
-    const int separator_height =
-        contents_separator_->GetPreferredSize().height();
-    contents_separator_->SetBounds(vertical_layout_rect_.x(), top,
-                                   vertical_layout_rect_.width(),
-                                   separator_height);
-    if (loading_bar_) {
-      SetViewVisibility(loading_bar_, true);
-      loading_bar_->SetBounds(vertical_layout_rect_.x(), top - 2,
-                              vertical_layout_rect_.width(),
-                              separator_height + 2);
-      top_container_->ReorderChildView(loading_bar_,
-                                       top_container_->children().size());
-    }
-    top += separator_height;
-  } else {
-    SetViewVisibility(contents_separator_, false);
-    if (loading_bar_) {
-      SetViewVisibility(loading_bar_, false);
-    }
+    SetViewVisibility(bookmark_bar_, false);
+    bookmark_bar_->SetBounds(0, top, browser_view_->width(), 0);
+  }
+  SetViewVisibility(contents_separator_, false);
+  if (loading_bar_) {
+    SetViewVisibility(loading_bar_, false);
   }
 
   return LayoutInfoBar(top);
@@ -728,6 +796,157 @@ void BrowserViewLayout::LayoutContentsContainerView(int top, int bottom) {
   gfx::Rect contents_container_bounds(vertical_layout_rect_.x(), top,
                                       vertical_layout_rect_.width(),
                                       std::max(0, bottom - top));
+
+  // Dao: Layout the left sidebar and add content insets
+  dao::DaoSidebarView* dao_sidebar = browser_view_->dao_sidebar();
+  bool dao_sidebar_active = dao_sidebar && dao_sidebar->GetVisible();
+  if (dao_sidebar_active) {
+    int sidebar_width = dao_sidebar->GetPreferredSize().width();
+    dao_sidebar->SetBounds(
+        contents_container_bounds.x(), contents_container_bounds.y(),
+        sidebar_width, contents_container_bounds.height());
+    contents_container_bounds.set_x(
+        contents_container_bounds.x() + sidebar_width);
+    contents_container_bounds.set_width(
+        std::max(0, contents_container_bounds.width() - sidebar_width));
+  }
+
+  // Dao: Layout right agent sidebar
+  auto* dao_agent_sidebar = browser_view_->dao_agent_sidebar();
+  if (dao_agent_sidebar && dao_agent_sidebar->GetVisible()) {
+    int agent_width = dao_agent_sidebar->GetPreferredSize().width();
+    dao_agent_sidebar->SetBounds(
+        contents_container_bounds.right() - agent_width,
+        contents_container_bounds.y(),
+        agent_width,
+        contents_container_bounds.height());
+    contents_container_bounds.set_width(
+        std::max(0, contents_container_bounds.width() - agent_width));
+  }
+
+  // Dao: Apply content insets (top, right, bottom) so the frame color
+  // shows through as a gap around the web content area
+  gfx::Rect dao_content_area = contents_container_bounds;
+  if (dao_sidebar_active) {
+    dao_content_area.Inset(
+        gfx::Insets::TLBR(dao::kContentInsetTop, 0,
+                           dao::kContentInsetBottom,
+                           dao::kContentInsetRight));
+  }
+
+  // Dao: Position address bar at top of content area
+  auto* address_bar = browser_view_->dao_address_bar();
+  int address_bar_height = 0;
+  if (address_bar && dao_sidebar_active) {
+    address_bar_height = dao::DaoAddressBarView::kBarHeight;
+    address_bar->SetBounds(dao_content_area.x(), dao_content_area.y(),
+                           dao_content_area.width(), address_bar_height);
+    address_bar->SetVisible(true);
+    // Shift content area down to make room for address bar
+    dao_content_area.Inset(gfx::Insets::TLBR(address_bar_height, 0, 0, 0));
+  } else if (address_bar) {
+    address_bar->SetVisible(false);
+  }
+
+  // Dao: Position corner overlay slightly larger than content for shadow
+  auto* corner_overlay = browser_view_->dao_corner_overlay();
+  if (corner_overlay) {
+    // Overlay covers address bar + content area
+    gfx::Rect overlay_content = dao_content_area;
+    overlay_content.Inset(gfx::Insets::TLBR(-address_bar_height, 0, 0, 0));
+    gfx::Rect overlay_bounds = overlay_content;
+    overlay_bounds.Inset(gfx::Insets(-dao::kContentShadowMargin));
+    corner_overlay->SetBoundsRect(overlay_bounds);
+  }
+
+  // Dao: Position command bar to match the content area (full, no inset)
+  auto* command_bar = browser_view_->dao_command_bar();
+  if (command_bar) {
+    command_bar->SetBoundsRect(contents_container_bounds);
+  }
+
+  // Dao: Position control center popup over the entire BrowserView so
+  // clicking anywhere outside the card closes it.
+  auto* cc_popup = browser_view_->dao_control_center_popup();
+  if (cc_popup) {
+    cc_popup->SetBounds(0, 0, browser_view_->width(),
+                         browser_view_->height());
+  }
+
+  // Dao: Download flyout covers the entire BrowserView for arc animation
+  auto* download_flyout = browser_view_->dao_download_flyout();
+  if (download_flyout) {
+    download_flyout->SetBounds(0, 0, browser_view_->width(),
+                               browser_view_->height());
+  }
+
+  // Dao: Apply rounded corners — only bottom corners on contents container
+  // (top corners are on the address bar view)
+  if (dao_sidebar_active) {
+    if (!contents_container_->layer()) {
+      contents_container_->SetPaintToLayer();
+      contents_container_->layer()->SetFillsBoundsOpaquely(false);
+    }
+    contents_container_->layer()->SetRoundedCornerRadius(
+        gfx::RoundedCornersF(0, 0,
+                              dao::kContentCornerRadius,
+                              dao::kContentCornerRadius));
+    contents_container_->layer()->SetIsFastRoundedCorner(true);
+  }
+
+  // Dao: Apply inset to the actual contents container
+  if (dao_sidebar_active) {
+    contents_container_bounds = dao_content_area;
+  }
+
+  // Dao: Little Dao layout — content insets on all 4 sides, top bar above content
+  auto* little_dao_view = browser_view_->dao_little_dao_view();
+  bool dao_little_dao_active = little_dao_view && little_dao_view->GetVisible();
+  if (dao_little_dao_active) {
+    // Apply insets on all sides (no sidebar, so left also gets inset)
+    gfx::Rect little_dao_area = contents_container_bounds;
+    little_dao_area.Inset(
+        gfx::Insets::TLBR(dao::kContentInsetTop, dao::kContentInsetBottom,
+                           dao::kContentInsetBottom, dao::kContentInsetRight));
+
+    // Position the Little Dao bar at the top
+    int bar_height = dao::DaoLittleDaoView::kBarHeight;
+    little_dao_view->SetBounds(little_dao_area.x(), little_dao_area.y(),
+                               little_dao_area.width(), bar_height);
+    little_dao_view->SetVisible(true);
+
+    // Shift content below the bar
+    little_dao_area.Inset(gfx::Insets::TLBR(bar_height, 0, 0, 0));
+
+    // Position corner overlay for Little Dao
+    auto* ld_corner_overlay = browser_view_->dao_corner_overlay();
+    if (ld_corner_overlay) {
+      gfx::Rect overlay_content = little_dao_area;
+      overlay_content.Inset(gfx::Insets::TLBR(-bar_height, 0, 0, 0));
+      gfx::Rect overlay_bounds = overlay_content;
+      overlay_bounds.Inset(gfx::Insets(-dao::kContentShadowMargin));
+      ld_corner_overlay->SetBoundsRect(overlay_bounds);
+    }
+
+    // Apply rounded corners to bottom of contents container
+    if (!contents_container_->layer()) {
+      contents_container_->SetPaintToLayer();
+      contents_container_->layer()->SetFillsBoundsOpaquely(false);
+    }
+    contents_container_->layer()->SetRoundedCornerRadius(
+        gfx::RoundedCornersF(0, 0, dao::kContentCornerRadius,
+                              dao::kContentCornerRadius));
+    contents_container_->layer()->SetIsFastRoundedCorner(true);
+
+    // Dao: Position command bar to cover the full Little Dao content area
+    auto* ld_command_bar = browser_view_->dao_command_bar();
+    if (ld_command_bar) {
+      ld_command_bar->SetBoundsRect(contents_container_bounds);
+    }
+
+    contents_container_bounds = little_dao_area;
+  }
+
   if (webui_tab_strip_ && webui_tab_strip_->GetVisible()) {
     // The WebUI tab strip container should "push" the tab contents down without
     // resizing it.
@@ -881,6 +1100,13 @@ void BrowserViewLayout::UpdateTopContainerBounds() {
   // layout and we assume that this is the case.
   height = std::max(height, delegate_->GetTopInsetInBrowserView());
 
+  // Dao: When toolbar is offscreen (y < 0) but visible, its bounds().bottom()
+  // is 0, giving top_container zero height. Immersive fullscreen needs a
+  // non-zero overlay widget size, so ensure minimum height from the toolbar.
+  if (height == 0 && toolbar_->GetVisible() && toolbar_->bounds().y() < 0) {
+    height = toolbar_->GetPreferredSize().height();
+  }
+
   gfx::Rect top_container_bounds(vertical_layout_rect_.width(), height);
 
   if (delegate_->IsTopControlsSlideBehaviorEnabled()) {
