diff --git a/chrome/browser/ui/views/user_education/browser_user_education_service.cc b/chrome/browser/ui/views/user_education/browser_user_education_service.cc
index 865c12b3e5..8d805c8eff 100644
--- a/chrome/browser/ui/views/user_education/browser_user_education_service.cc
+++ b/chrome/browser/ui/views/user_education/browser_user_education_service.cc
@@ -152,12 +152,15 @@
 namespace {
 
 using Platforms = user_education::Metadata::Platforms;
-constexpr std::initializer_list<Platforms> kComposePlatforms{
+// Dao: marked maybe_unused — only referenced by the disabled upstream IPHs.
+[[maybe_unused]] constexpr std::initializer_list<Platforms> kComposePlatforms{
     Platforms::kWindows, Platforms::kMac, Platforms::kLinux};
 
-constexpr char kTabGroupHeaderElementName[] = "TabGroupHeader";
-constexpr char kChromeThemeBackElementName[] = "ChromeThemeBackElement";
-constexpr char kLastInactiveTabElementName[] = "LastInactiveTab";
+// Dao: marked maybe_unused — only referenced by the disabled upstream IPHs/tutorials.
+[[maybe_unused]] constexpr char kTabGroupHeaderElementName[] = "TabGroupHeader";
+[[maybe_unused]] constexpr char kChromeThemeBackElementName[] =
+    "ChromeThemeBackElement";
+[[maybe_unused]] constexpr char kLastInactiveTabElementName[] = "LastInactiveTab";
 
 class IfView : public user_education::TutorialDescription::If {
  public:
@@ -229,7 +232,8 @@ class ConditionalStep : public IfView {
   }
 };
 
-bool HasTabGroups(const BrowserView* browser_view) {
+// Dao: marked maybe_unused — only referenced by the disabled upstream tutorials.
+[[maybe_unused]] bool HasTabGroups(const BrowserView* browser_view) {
   return !browser_view->browser()
               ->tab_strip_model()
               ->group_model()
@@ -237,7 +241,8 @@ bool HasTabGroups(const BrowserView* browser_view) {
               .empty();
 }
 
-bool IsInVerticalTabsMode(const BrowserView* browser_view) {
+// Dao: marked maybe_unused — only referenced by the disabled upstream tutorials.
+[[maybe_unused]] bool IsInVerticalTabsMode(const BrowserView* browser_view) {
   return browser_view->ShouldDrawVerticalTabStrip();
 }
 
@@ -256,7 +261,8 @@ Browser* GetBrowser(ContextPtr context) {
 }
 
 // Returns a `CustomActionCallback` that navigates to `target` in a new tab.
-user_education::FeaturePromoSpecification::CustomActionCallback
+// Dao: marked maybe_unused — only referenced by the disabled upstream IPHs.
+[[maybe_unused]] user_education::FeaturePromoSpecification::CustomActionCallback
 CreateNavigationAction(GURL target) {
   return base::BindRepeating(
       [](GURL url, ContextPtr ctx,
@@ -304,6 +310,12 @@ void RegisterChromeHelpBubbleFactories(
 void MaybeRegisterChromeFeaturePromos(
     user_education::FeaturePromoRegistry& registry,
     Profile* profile) {
+  // Dao: disable all upstream In-Product-Help (IPH) feature promos.
+  // Dao ships its own onboarding/affordances and the upstream IPHs anchor
+  // to Chrome UI elements (toolbar buttons, tab strip, etc.) that Dao hides
+  // or replaces, so they would either misrender or never display.
+  return;
+#if 0  // Dao: upstream IPH registrations disabled below.
   using user_education::FeaturePromoSpecification;
   using user_education::HelpBubbleArrow;
   using user_education::Metadata;
@@ -1829,6 +1841,7 @@ void MaybeRegisterChromeFeaturePromos(
           .SetMetadata(148, "charlesmeng@chromium.org",
                        "Triggered when the vertical tabs is enabled and the "
                        "user has not enabled expand on hover before.")));
+#endif  // Dao: upstream IPH registrations disabled.
 }
 
 void MaybeRegisterChromeFeaturePromos(
@@ -1838,6 +1851,10 @@ void MaybeRegisterChromeFeaturePromos(
 
 void MaybeRegisterChromeTutorials(
     user_education::TutorialRegistry& tutorial_registry) {
+  // Dao: disable all upstream tutorials (Tab Group, Side Panel, Lens,
+  // Split View, Vertical Tabs, etc.) — Dao provides its own guidance.
+  return;
+#if 0  // Dao: upstream tutorial registrations disabled below.
   using user_education::HelpBubbleArrow;
   using user_education::TutorialDescription;
   using BubbleStep = user_education::TutorialDescription::BubbleStep;
@@ -2179,6 +2196,7 @@ void MaybeRegisterChromeTutorials(
     tutorial_registry.AddTutorial(kVerticalTabsTutorialId,
                                   std::move(vertical_tabs_tutorial));
   }
+#endif  // Dao: upstream tutorial registrations disabled.
 }
 
 // NOTES FOR FEATURE TEAMS:
