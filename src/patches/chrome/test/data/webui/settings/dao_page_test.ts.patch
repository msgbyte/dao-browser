diff --git a/chrome/test/data/webui/settings/dao_page_test.ts b/chrome/test/data/webui/settings/dao_page_test.ts
new file mode 100644
index 0000000000..0000000001
--- /dev/null
+++ b/chrome/test/data/webui/settings/dao_page_test.ts
@@ -0,0 +1,1111 @@
+// Copyright 2026 Dao Browser Authors. All rights reserved.
+// Use of this source code is governed by a BSD-style license that can be
+// found in the LICENSE file.
+
+import 'chrome://settings/settings.js';
+
+import type {CrInputElement} from 'chrome://resources/cr_elements/cr_input/cr_input.js';
+import {webUIListenerCallback} from 'chrome://resources/js/cr.js';
+import {flush} from 'chrome://resources/polymer/v3_0/polymer/polymer_bundled.min.js';
+import type {DaoAgentSettingsBrowserProxy, DaoAgentSettingsSnapshot, DaoMcpBrowserProxy, DaoMcpStatus, SettingsDaoPageElement, SettingsToggleButtonElement} from 'chrome://settings/settings.js';
+import {DaoAgentSettingsBrowserProxyImpl, DaoMcpBrowserProxyImpl, loadTimeData, Router, routes} from 'chrome://settings/settings.js';
+import {assertDeepEquals, assertEquals, assertFalse, assertNull, assertTrue} from 'chrome://webui-test/chai_assert.js';
+import {flushTasks} from 'chrome://webui-test/polymer_test_util.js';
+import {TestBrowserProxy} from 'chrome://webui-test/test_browser_proxy.js';
+
+type DaoMcpSetupOption = 'codex'|'claude-code'|'generic-mcp';
+type DaoAgentMemorySummary =
+    NonNullable<SettingsDaoPageElement['memorySummaryForTest']>;
+type DaoAgentWorkspaceSummary =
+    NonNullable<SettingsDaoPageElement['workspaceSummaryForTest']>;
+type DaoAgentUsageStats =
+    Awaited<ReturnType<DaoAgentSettingsBrowserProxy['getUsageStats']>>;
+
+interface Deferred<T> {
+  resolve: (value: T) => void;
+  reject: (reason?: unknown) => void;
+}
+
+const memorySummary: DaoAgentMemorySummary = {
+  totalSize: 1536,
+  conversationCount: 12,
+  episodeCount: 4,
+  preferenceCount: 3,
+};
+
+const workspaceSummary: DaoAgentWorkspaceSummary = {
+  root: '/Users/test/Dao Agent Workspace',
+  usedBytes: 4096,
+  capBytes: 1048576,
+  fileCount: 7,
+  fileCountCap: 100,
+  recentActivity: [{
+    timestamp: '2026-08-07T01:02:03.000Z',
+    operation: 'read',
+    path: 'notes/example.md',
+  }],
+};
+
+const usageStats: DaoAgentUsageStats = {
+  apiCalls: 9,
+  toolCalls: {workspace_read: 5},
+  promptTokens: 1200,
+  completionTokens: 300,
+  totalTokens: 1500,
+  estimatedCost: 0.000123,
+  lastReset: 1786064523000,
+};
+
+class TestDaoAgentSettingsBrowserProxy extends TestBrowserProxy implements
+    DaoAgentSettingsBrowserProxy {
+  private snapshot_: DaoAgentSettingsSnapshot = {
+    migrationVersion: 2,
+    values: {
+      dao_agent_active_provider: 'anthropic',
+      dao_agent_providers: JSON.stringify({
+        anthropic: {apiKey: 'secret', baseUrl: '', model: 'claude-test'},
+      }),
+      dao_agent_memory_enabled: 'true',
+      dao_dream_enabled: 'false',
+    },
+    usageStats: {
+      apiCalls: 0,
+      toolCalls: {},
+      promptTokens: 0,
+      completionTokens: 0,
+      totalTokens: 0,
+      estimatedCost: 0,
+      lastReset: 0,
+    },
+  };
+  private memorySummaryRequests_: Array<Deferred<unknown>> = [];
+  private workspaceSummaryRequests_: Array<Deferred<unknown>> = [];
+  private usageStatsRequests_: Array<Deferred<DaoAgentUsageStats>> = [];
+  private clearMemoryRequests_: Array<Deferred<boolean>> = [];
+  private resetUsageRequests_: Array<Deferred<boolean>> = [];
+
+  constructor() {
+    super([
+      'getSettings',
+      'setSetting',
+      'getMemorySummary',
+      'clearAllMemory',
+      'getWorkspaceSummary',
+      'openWorkspace',
+      'getUsageStats',
+      'resetUsageStats',
+    ]);
+  }
+
+  getSettings(): Promise<DaoAgentSettingsSnapshot> {
+    this.methodCalled('getSettings');
+    return Promise.resolve(this.snapshot_);
+  }
+
+  setSetting(key: string, value: string|null): Promise<boolean> {
+    this.methodCalled('setSetting', key, value);
+    return Promise.resolve(true);
+  }
+
+  getMemorySummary(): Promise<unknown> {
+    this.methodCalled('getMemorySummary');
+    return new Promise((resolve, reject) => {
+      this.memorySummaryRequests_.push({resolve, reject});
+    });
+  }
+
+  clearAllMemory(): Promise<boolean> {
+    this.methodCalled('clearAllMemory');
+    return new Promise((resolve, reject) => {
+      this.clearMemoryRequests_.push({resolve, reject});
+    });
+  }
+
+  getWorkspaceSummary(): Promise<unknown> {
+    this.methodCalled('getWorkspaceSummary');
+    return new Promise((resolve, reject) => {
+      this.workspaceSummaryRequests_.push({resolve, reject});
+    });
+  }
+
+  openWorkspace(): Promise<boolean> {
+    this.methodCalled('openWorkspace');
+    return Promise.resolve(true);
+  }
+
+  getUsageStats(): Promise<DaoAgentUsageStats> {
+    this.methodCalled('getUsageStats');
+    return new Promise((resolve, reject) => {
+      this.usageStatsRequests_.push({resolve, reject});
+    });
+  }
+
+  resetUsageStats(): Promise<boolean> {
+    this.methodCalled('resetUsageStats');
+    return new Promise((resolve, reject) => {
+      this.resetUsageRequests_.push({resolve, reject});
+    });
+  }
+
+  resolveMemorySummary(summary: unknown, request = 0): void {
+    const deferred = this.memorySummaryRequests_[request];
+    assertTrue(!!deferred);
+    this.memorySummaryRequests_.splice(request, 1);
+    deferred.resolve(summary);
+  }
+
+  rejectMemorySummary(request = 0): void {
+    const deferred = this.memorySummaryRequests_[request];
+    assertTrue(!!deferred);
+    this.memorySummaryRequests_.splice(request, 1);
+    deferred.reject(new Error('memory summary failed'));
+  }
+
+  resolveWorkspaceSummary(summary: unknown, request = 0): void {
+    const deferred = this.workspaceSummaryRequests_[request];
+    assertTrue(!!deferred);
+    this.workspaceSummaryRequests_.splice(request, 1);
+    deferred.resolve(summary);
+  }
+
+  rejectWorkspaceSummary(request = 0): void {
+    const deferred = this.workspaceSummaryRequests_[request];
+    assertTrue(!!deferred);
+    this.workspaceSummaryRequests_.splice(request, 1);
+    deferred.reject(new Error('workspace summary failed'));
+  }
+
+  resolveUsageStats(stats: DaoAgentUsageStats, request = 0): void {
+    const deferred = this.usageStatsRequests_[request];
+    assertTrue(!!deferred);
+    this.usageStatsRequests_.splice(request, 1);
+    deferred.resolve(stats);
+  }
+
+  rejectUsageStats(request = 0): void {
+    const deferred = this.usageStatsRequests_[request];
+    assertTrue(!!deferred);
+    this.usageStatsRequests_.splice(request, 1);
+    deferred.reject(new Error('usage stats failed'));
+  }
+
+  resolveClearAllMemory(success: boolean): void {
+    const deferred = this.clearMemoryRequests_.shift();
+    assertTrue(!!deferred);
+    deferred.resolve(success);
+  }
+
+  rejectClearAllMemory(): void {
+    const deferred = this.clearMemoryRequests_.shift();
+    assertTrue(!!deferred);
+    deferred.reject(new Error('clear memory failed'));
+  }
+
+  resolveResetUsageStats(success: boolean): void {
+    const deferred = this.resetUsageRequests_.shift();
+    assertTrue(!!deferred);
+    deferred.resolve(success);
+  }
+
+  rejectResetUsageStats(): void {
+    const deferred = this.resetUsageRequests_.shift();
+    assertTrue(!!deferred);
+    deferred.reject(new Error('reset usage failed'));
+  }
+}
+
+class TestDaoMcpBrowserProxy extends TestBrowserProxy implements
+    DaoMcpBrowserProxy {
+  private status_: DaoMcpStatus = {
+    enabled: false,
+    state: 'disabled',
+    canStop: false,
+  };
+  private setupContentResolvers_ =
+      new Map<DaoMcpSetupOption, (content: string) => void>();
+  private copyContentResolvers_ =
+      new Map<DaoMcpSetupOption, (copied: boolean) => void>();
+
+  constructor() {
+    super([
+      'getDaoMcpStatus',
+      'setDaoMcpEnabled',
+      'getDaoMcpSetupContent',
+      'copyDaoMcpSetupContent',
+      'stopDaoMcpControl',
+    ]);
+  }
+
+  getDaoMcpStatus(): Promise<DaoMcpStatus> {
+    this.methodCalled('getDaoMcpStatus');
+    return Promise.resolve(this.status_);
+  }
+
+  setDaoMcpEnabled(enabled: boolean): void {
+    this.methodCalled('setDaoMcpEnabled', enabled);
+  }
+
+  getDaoMcpSetupContent(option: DaoMcpSetupOption): Promise<string> {
+    this.methodCalled('getDaoMcpSetupContent', option);
+    return new Promise(resolve => {
+      this.setupContentResolvers_.set(option, resolve);
+    });
+  }
+
+  copyDaoMcpSetupContent(option: DaoMcpSetupOption): Promise<boolean> {
+    this.methodCalled('copyDaoMcpSetupContent', option);
+    return new Promise(resolve => {
+      this.copyContentResolvers_.set(option, resolve);
+    });
+  }
+
+  resolveSetupContent(option: DaoMcpSetupOption, content: string) {
+    const resolve = this.setupContentResolvers_.get(option);
+    assertTrue(!!resolve);
+    this.setupContentResolvers_.delete(option);
+    resolve(content);
+  }
+
+  resolveCopyContent(option: DaoMcpSetupOption, copied: boolean) {
+    const resolve = this.copyContentResolvers_.get(option);
+    assertTrue(!!resolve);
+    this.copyContentResolvers_.delete(option);
+    resolve(copied);
+  }
+
+  stopDaoMcpControl(): void {
+    this.methodCalled('stopDaoMcpControl');
+  }
+}
+
+suite('DaoPage', function() {
+  let page: SettingsDaoPageElement;
+  let mcpBrowserProxy: TestDaoMcpBrowserProxy;
+  let agentSettingsBrowserProxy: TestDaoAgentSettingsBrowserProxy;
+
+  setup(async function() {
+    mcpBrowserProxy = new TestDaoMcpBrowserProxy();
+    DaoMcpBrowserProxyImpl.setInstance(mcpBrowserProxy);
+    agentSettingsBrowserProxy = new TestDaoAgentSettingsBrowserProxy();
+    DaoAgentSettingsBrowserProxyImpl.setInstance(agentSettingsBrowserProxy);
+    document.body.innerHTML = window.trustedTypes!.emptyHTML;
+    page = document.createElement('settings-dao-page');
+    page.prefs = {
+      dao: {
+        enhanced_command_bar_suggestions_enabled: {
+          key: 'dao.enhanced_command_bar_suggestions_enabled',
+          type: chrome.settingsPrivate.PrefType.BOOLEAN,
+          value: false,
+        },
+        ask_ai_enabled: {
+          key: 'dao.ask_ai_enabled',
+          type: chrome.settingsPrivate.PrefType.BOOLEAN,
+          value: true,
+        },
+        stale_tab_expiration_hours: {
+          key: 'dao.stale_tab_expiration_hours',
+          type: chrome.settingsPrivate.PrefType.NUMBER,
+          value: 24,
+        },
+        enhanced_pip_enabled: {
+          key: 'dao.enhanced_pip_enabled',
+          type: chrome.settingsPrivate.PrefType.BOOLEAN,
+          value: true,
+        },
+        little_dao_enabled: {
+          key: 'dao.little_dao_enabled',
+          type: chrome.settingsPrivate.PrefType.BOOLEAN,
+          value: true,
+        },
+      },
+    };
+    document.body.appendChild(page);
+    await mcpBrowserProxy.whenCalled('getDaoMcpStatus');
+    await agentSettingsBrowserProxy.whenCalled('getSettings');
+    await flushTasks();
+    flush();
+  });
+
+  teardown(function() {
+    page.remove();
+  });
+
+  function getMcpToggle(): SettingsToggleButtonElement {
+    const toggle =
+        page.shadowRoot!.querySelector<SettingsToggleButtonElement>(
+            '#daoMcpServerEnabled');
+    assertTrue(!!toggle);
+    return toggle;
+  }
+
+  function sendMcpStatus(status: DaoMcpStatus) {
+    webUIListenerCallback('dao-mcp-status-changed', status);
+    flush();
+  }
+
+  async function resolveAllManagementSummaries(): Promise<void> {
+    agentSettingsBrowserProxy.resolveMemorySummary(memorySummary);
+    agentSettingsBrowserProxy.resolveWorkspaceSummary(workspaceSummary);
+    agentSettingsBrowserProxy.resolveUsageStats(usageStats);
+    await flushTasks();
+    flush();
+  }
+
+  async function reconnectPage(): Promise<void> {
+    page.remove();
+    document.body.appendChild(page);
+    await flushTasks();
+    flush();
+  }
+
+  test('agentManagementSummariesLoadIndependently', async function() {
+    assertEquals(
+        1, agentSettingsBrowserProxy.getCallCount('getMemorySummary'));
+    assertEquals(
+        1, agentSettingsBrowserProxy.getCallCount('getWorkspaceSummary'));
+    assertEquals(1, agentSettingsBrowserProxy.getCallCount('getUsageStats'));
+    assertTrue(page.memoryLoadingForTest);
+    assertTrue(page.workspaceLoadingForTest);
+    assertTrue(page.usageLoadingForTest);
+
+    agentSettingsBrowserProxy.resolveMemorySummary(memorySummary);
+    await flushTasks();
+
+    assertFalse(page.memoryLoadingForTest);
+    assertTrue(page.workspaceLoadingForTest);
+    assertTrue(page.usageLoadingForTest);
+    assertDeepEquals(memorySummary, page.memorySummaryForTest);
+  });
+
+  test('memoryFailureDoesNotReplaceOtherManagementCards', async function() {
+    agentSettingsBrowserProxy.rejectMemorySummary();
+    agentSettingsBrowserProxy.resolveWorkspaceSummary(workspaceSummary);
+    agentSettingsBrowserProxy.resolveUsageStats(usageStats);
+    await flushTasks();
+    flush();
+
+    assertTrue(page.memoryErrorForTest);
+    assertFalse(page.workspaceErrorForTest);
+    assertFalse(page.usageErrorForTest);
+    assertDeepEquals(workspaceSummary, page.workspaceSummaryForTest);
+    assertDeepEquals(usageStats, page.usageStatsForTest);
+    assertTrue(!!page.shadowRoot!.querySelector('#agentMemoryRetry'));
+    assertNull(page.shadowRoot!.querySelector('#agentWorkspaceRetry'));
+    assertNull(page.shadowRoot!.querySelector('#agentUsageRetry'));
+  });
+
+  test('workspaceFailureDoesNotReplaceOtherManagementCards',
+      async function() {
+        agentSettingsBrowserProxy.resolveMemorySummary(memorySummary);
+        agentSettingsBrowserProxy.rejectWorkspaceSummary();
+        agentSettingsBrowserProxy.resolveUsageStats(usageStats);
+        await flushTasks();
+        flush();
+
+        assertFalse(page.memoryErrorForTest);
+        assertTrue(page.workspaceErrorForTest);
+        assertFalse(page.usageErrorForTest);
+        assertDeepEquals(memorySummary, page.memorySummaryForTest);
+        assertDeepEquals(usageStats, page.usageStatsForTest);
+        assertNull(page.shadowRoot!.querySelector('#agentMemoryRetry'));
+        assertTrue(!!page.shadowRoot!.querySelector('#agentWorkspaceRetry'));
+        assertNull(page.shadowRoot!.querySelector('#agentUsageRetry'));
+      });
+
+  test('usageFailureDoesNotReplaceOtherManagementCards', async function() {
+    agentSettingsBrowserProxy.resolveMemorySummary(memorySummary);
+    agentSettingsBrowserProxy.resolveWorkspaceSummary(workspaceSummary);
+    agentSettingsBrowserProxy.rejectUsageStats();
+    await flushTasks();
+    flush();
+
+    assertFalse(page.memoryErrorForTest);
+    assertFalse(page.workspaceErrorForTest);
+    assertTrue(page.usageErrorForTest);
+    assertDeepEquals(memorySummary, page.memorySummaryForTest);
+    assertDeepEquals(workspaceSummary, page.workspaceSummaryForTest);
+    assertNull(page.shadowRoot!.querySelector('#agentMemoryRetry'));
+    assertNull(page.shadowRoot!.querySelector('#agentWorkspaceRetry'));
+    assertTrue(!!page.shadowRoot!.querySelector('#agentUsageRetry'));
+  });
+
+  test('emptyMemoryResponseIsFailureAndPreservesLastGoodData',
+      async function() {
+        await resolveAllManagementSummaries();
+        await reconnectPage();
+
+        agentSettingsBrowserProxy.resolveMemorySummary({});
+        agentSettingsBrowserProxy.resolveWorkspaceSummary(workspaceSummary);
+        agentSettingsBrowserProxy.resolveUsageStats(usageStats);
+        await flushTasks();
+
+        assertTrue(page.memoryErrorForTest);
+        assertDeepEquals(memorySummary, page.memorySummaryForTest);
+      });
+
+  test('emptyWorkspaceResponseIsFailureAndPreservesLastGoodData',
+      async function() {
+        await resolveAllManagementSummaries();
+        await reconnectPage();
+
+        agentSettingsBrowserProxy.resolveMemorySummary(memorySummary);
+        agentSettingsBrowserProxy.resolveWorkspaceSummary({});
+        agentSettingsBrowserProxy.resolveUsageStats(usageStats);
+        await flushTasks();
+
+        assertTrue(page.workspaceErrorForTest);
+        assertDeepEquals(workspaceSummary, page.workspaceSummaryForTest);
+      });
+
+  test('workspaceReconnectFailurePreservesLastGoodData', async function() {
+    await resolveAllManagementSummaries();
+    await reconnectPage();
+
+    agentSettingsBrowserProxy.resolveMemorySummary(memorySummary);
+    agentSettingsBrowserProxy.rejectWorkspaceSummary();
+    agentSettingsBrowserProxy.resolveUsageStats(usageStats);
+    await flushTasks();
+
+    assertEquals(
+        2, agentSettingsBrowserProxy.getCallCount('getWorkspaceSummary'));
+    assertTrue(page.workspaceErrorForTest);
+    assertDeepEquals(workspaceSummary, page.workspaceSummaryForTest);
+  });
+
+  test('usageEventWinsOverStaleInitialResponse', async function() {
+    const newerStats = {...usageStats, apiCalls: 10};
+    webUIListenerCallback('dao-agent-usage-stats-changed', newerStats);
+    agentSettingsBrowserProxy.resolveUsageStats(usageStats);
+    await flushTasks();
+
+    assertDeepEquals(newerStats, page.usageStatsForTest);
+    assertFalse(page.usageLoadingForTest);
+  });
+
+  test('cancelClearMemoryDoesNotCallNativeAndRestoresFocus',
+      async function() {
+        await resolveAllManagementSummaries();
+        const clearButton = page.shadowRoot!.querySelector<HTMLElement>(
+            '#clearAllMemoryButton')!;
+        const clearDialog = page.shadowRoot!.querySelector<HTMLElement & {
+          open: boolean,
+        }>('#clearAllMemoryDialog')!;
+
+        clearButton.click();
+        assertTrue(clearDialog.open);
+        clearDialog.querySelector<HTMLElement>('#clearAllMemoryCancel')!
+            .click();
+
+        assertEquals(
+            0, agentSettingsBrowserProxy.getCallCount('clearAllMemory'));
+        assertEquals(clearButton, page.shadowRoot!.activeElement);
+      });
+
+  test('clearSuccessRefreshFailureDoesNotSuggestClearingAgain',
+      async function() {
+        await resolveAllManagementSummaries();
+        const clearButton = page.shadowRoot!.querySelector<HTMLElement>(
+            '#clearAllMemoryButton')!;
+        const clearDialog = page.shadowRoot!.querySelector<HTMLElement & {
+          open: boolean,
+        }>('#clearAllMemoryDialog')!;
+        const confirm = clearDialog.querySelector<HTMLElement & {
+          disabled: boolean,
+        }>('#clearAllMemoryConfirm')!;
+
+        clearButton.click();
+        confirm.click();
+        await flushTasks();
+        assertEquals(
+            1, agentSettingsBrowserProxy.getCallCount('clearAllMemory'));
+        assertTrue(page.clearMemoryPendingForTest);
+        assertTrue(confirm.disabled);
+
+        agentSettingsBrowserProxy.resolveClearAllMemory(true);
+        await flushTasks();
+        assertFalse(clearDialog.open);
+        assertEquals(clearButton, page.shadowRoot!.activeElement);
+        assertEquals(
+            2, agentSettingsBrowserProxy.getCallCount('getMemorySummary'));
+        assertEquals(
+            1, agentSettingsBrowserProxy.getCallCount('getWorkspaceSummary'));
+        assertEquals(
+            1, agentSettingsBrowserProxy.getCallCount('getUsageStats'));
+
+        agentSettingsBrowserProxy.rejectMemorySummary();
+        await flushTasks();
+        flush();
+        assertTrue(page.memoryErrorForTest);
+        assertDeepEquals(memorySummary, page.memorySummaryForTest);
+        assertNull(page.shadowRoot!.querySelector('#memoryActionError'));
+        assertNull(page.shadowRoot!.querySelector('#memoryActionSuccess'));
+        const refreshError = page.shadowRoot!.querySelector<HTMLElement>(
+            '#memoryRefreshErrorMessage');
+        assertTrue(!!refreshError);
+        assertEquals(
+            loadTimeData.getString('daoAgentManagementMemoryRefreshError'),
+            refreshError.textContent.trim());
+      });
+
+  test('clearFailureUsesActionSpecificFeedback', async function() {
+    await resolveAllManagementSummaries();
+    const clearDialog = page.shadowRoot!.querySelector<HTMLElement>(
+        '#clearAllMemoryDialog')!;
+    page.shadowRoot!.querySelector<HTMLElement>('#clearAllMemoryButton')!
+        .click();
+    clearDialog.querySelector<HTMLElement>('#clearAllMemoryConfirm')!.click();
+    agentSettingsBrowserProxy.resolveClearAllMemory(false);
+    await flushTasks();
+    flush();
+
+    assertFalse(page.memoryErrorForTest);
+    assertTrue(page.memoryActionErrorForTest);
+    assertNull(page.shadowRoot!.querySelector('#memoryRefreshError'));
+    assertNull(page.shadowRoot!.querySelector('#agentMemoryRetry'));
+    const actionError = page.shadowRoot!.querySelector<HTMLElement>(
+        '#memoryActionError');
+    assertTrue(!!actionError);
+    assertEquals(
+        loadTimeData.getString('daoAgentManagementMemoryActionError'),
+        actionError.textContent.trim());
+  });
+
+  test('openWorkspaceCallsOnlyWorkspaceAction', async function() {
+    await resolveAllManagementSummaries();
+    page.shadowRoot!
+        .querySelector<HTMLElement>('#openAgentWorkspaceButton')!.click();
+    await agentSettingsBrowserProxy.whenCalled('openWorkspace');
+
+    assertEquals(1, agentSettingsBrowserProxy.getCallCount('openWorkspace'));
+    assertEquals(
+        1, agentSettingsBrowserProxy.getCallCount('getWorkspaceSummary'));
+  });
+
+  test('cancelResetUsageDoesNotCallNativeAndRestoresFocus',
+      async function() {
+        await resolveAllManagementSummaries();
+        const resetButton = page.shadowRoot!.querySelector<HTMLElement>(
+            '#resetUsageStatsButton')!;
+        const resetDialog = page.shadowRoot!.querySelector<HTMLElement & {
+          open: boolean,
+        }>('#resetUsageStatsDialog')!;
+
+        resetButton.click();
+        assertTrue(resetDialog.open);
+        resetDialog.querySelector<HTMLElement>('#resetUsageStatsCancel')!
+            .click();
+
+        assertEquals(
+            0, agentSettingsBrowserProxy.getCallCount('resetUsageStats'));
+        assertEquals(resetButton, page.shadowRoot!.activeElement);
+      });
+
+  test('resetSuccessRefreshFailureDoesNotSuggestResettingAgain',
+      async function() {
+        await resolveAllManagementSummaries();
+        const resetButton = page.shadowRoot!.querySelector<HTMLElement>(
+            '#resetUsageStatsButton')!;
+        const resetDialog = page.shadowRoot!.querySelector<HTMLElement & {
+          open: boolean,
+        }>('#resetUsageStatsDialog')!;
+        const confirm = resetDialog.querySelector<HTMLElement & {
+          disabled: boolean,
+        }>('#resetUsageStatsConfirm')!;
+
+        resetButton.click();
+        confirm.click();
+        await flushTasks();
+        assertEquals(
+            1, agentSettingsBrowserProxy.getCallCount('resetUsageStats'));
+        assertTrue(page.resetUsagePendingForTest);
+        assertTrue(confirm.disabled);
+
+        agentSettingsBrowserProxy.resolveResetUsageStats(true);
+        await flushTasks();
+        assertFalse(resetDialog.open);
+        assertEquals(resetButton, page.shadowRoot!.activeElement);
+        assertEquals(
+            1, agentSettingsBrowserProxy.getCallCount('getMemorySummary'));
+        assertEquals(
+            1, agentSettingsBrowserProxy.getCallCount('getWorkspaceSummary'));
+        assertEquals(2, agentSettingsBrowserProxy.getCallCount('getUsageStats'));
+        agentSettingsBrowserProxy.rejectUsageStats();
+        await flushTasks();
+        flush();
+        assertTrue(page.usageErrorForTest);
+        assertDeepEquals(usageStats, page.usageStatsForTest);
+        assertNull(page.shadowRoot!.querySelector('#usageActionError'));
+        assertNull(page.shadowRoot!.querySelector('#usageActionSuccess'));
+        const refreshError = page.shadowRoot!.querySelector<HTMLElement>(
+            '#usageRefreshErrorMessage');
+        assertTrue(!!refreshError);
+        assertEquals(
+            loadTimeData.getString('daoAgentManagementUsageRefreshError'),
+            refreshError.textContent.trim());
+      });
+
+  test('resetFailureUsesActionSpecificFeedback', async function() {
+    await resolveAllManagementSummaries();
+    const resetDialog = page.shadowRoot!.querySelector<HTMLElement>(
+        '#resetUsageStatsDialog')!;
+    page.shadowRoot!.querySelector<HTMLElement>('#resetUsageStatsButton')!
+        .click();
+    resetDialog.querySelector<HTMLElement>('#resetUsageStatsConfirm')!.click();
+    agentSettingsBrowserProxy.rejectResetUsageStats();
+    await flushTasks();
+    flush();
+
+    assertFalse(page.usageErrorForTest);
+    assertTrue(page.usageActionErrorForTest);
+    assertNull(page.shadowRoot!.querySelector('#usageRefreshError'));
+    assertNull(page.shadowRoot!.querySelector('#agentUsageRetry'));
+    const actionError = page.shadowRoot!.querySelector<HTMLElement>(
+        '#usageActionError');
+    assertTrue(!!actionError);
+    assertEquals(
+        loadTimeData.getString('daoAgentManagementUsageActionError'),
+        actionError.textContent.trim());
+  });
+
+  test('newerMemoryReloadWinsOverStaleResponse', async function() {
+    await resolveAllManagementSummaries();
+    const clearButton = page.shadowRoot!.querySelector<HTMLElement>(
+        '#clearAllMemoryButton')!;
+    const clearDialog = page.shadowRoot!.querySelector<HTMLElement>(
+        '#clearAllMemoryDialog')!;
+
+    clearButton.click();
+    clearDialog.querySelector<HTMLElement>('#clearAllMemoryConfirm')!.click();
+    agentSettingsBrowserProxy.resolveClearAllMemory(true);
+    await flushTasks();
+    clearButton.click();
+    clearDialog.querySelector<HTMLElement>('#clearAllMemoryConfirm')!.click();
+    agentSettingsBrowserProxy.resolveClearAllMemory(true);
+    await flushTasks();
+
+    const newestSummary = {...memorySummary, conversationCount: 1};
+    agentSettingsBrowserProxy.resolveMemorySummary(newestSummary, 1);
+    await flushTasks();
+    agentSettingsBrowserProxy.resolveMemorySummary(memorySummary);
+    await flushTasks();
+
+    assertDeepEquals(newestSummary, page.memorySummaryForTest);
+    assertFalse(page.memoryLoadingForTest);
+  });
+
+  test('agentSettingsLoadFromProfileSnapshot', function() {
+    const provider = page.shadowRoot!.querySelector<HTMLSelectElement>(
+        '#daoAgentProvider');
+    const model =
+        page.shadowRoot!.querySelector<CrInputElement>('#daoAgentModel');
+    const memory = page.shadowRoot!.querySelector<HTMLElement & {
+      checked: boolean,
+    }>('[data-setting="dao_agent_memory_enabled"]');
+    const dream = page.shadowRoot!.querySelector<HTMLElement & {
+      checked: boolean,
+    }>('[data-setting="dao_dream_enabled"]');
+    assertTrue(!!provider);
+    assertTrue(!!model);
+    assertTrue(!!memory);
+    assertTrue(!!dream);
+    assertEquals('anthropic', provider.value);
+    assertEquals('claude-test', model.value);
+    assertTrue(memory.checked);
+    assertFalse(dream.checked);
+  });
+
+  test('agentSearchOptionsUseRuntimeOverrideValues', function() {
+    const search = page.shadowRoot!.querySelector<HTMLSelectElement>(
+        '#daoAgentSearchSource');
+    assertTrue(!!search);
+    assertEquals(
+        'auto,provider,duckduckgo',
+        [...search.options].map(option => option.value).join(','));
+  });
+
+  test('agentSettingChangesWriteThroughProfileProxy', async function() {
+    const memory = page.shadowRoot!.querySelector<HTMLElement & {
+      checked: boolean,
+    }>('[data-setting="dao_agent_memory_enabled"]');
+    assertTrue(!!memory);
+    memory.checked = false;
+    memory.dispatchEvent(new Event('change'));
+
+    const args = await agentSettingsBrowserProxy.whenCalled('setSetting');
+    assertEquals('dao_agent_memory_enabled', args[0]);
+    assertEquals('false', args[1]);
+  });
+
+  test('mcpMasterSwitchDefaultsOff', function() {
+    const toggle = getMcpToggle();
+    assertEquals('dao.mcp_server_enabled', toggle.pref!.key);
+    assertFalse(toggle.checked);
+  });
+
+  test('mcpMasterSwitchSendsRequestedState', async function() {
+    const toggle = getMcpToggle();
+    toggle.checked = true;
+    toggle.notifyChangedByUserInteraction();
+
+    assertTrue(await mcpBrowserProxy.whenCalled('setDaoMcpEnabled'));
+  });
+
+  test('mcpRendersDisabledReadyAndApprovalRequestedStates', function() {
+    const status =
+        page.shadowRoot!.querySelector<HTMLElement>('#daoMcpStatusValue');
+    assertTrue(!!status);
+    assertEquals(loadTimeData.getString('daoMcpStatusDisabled'), status.textContent.trim());
+
+    sendMcpStatus({
+      enabled: true,
+      state: 'ready',
+      canStop: false,
+    });
+    assertEquals(loadTimeData.getString('daoMcpStatusReady'), status.textContent.trim());
+
+    sendMcpStatus({
+      enabled: true,
+      state: 'approval_requested',
+      canStop: false,
+    });
+    assertEquals(
+        loadTimeData.getString('daoMcpStatusApprovalRequested'),
+        status.textContent.trim());
+    assertNull(page.shadowRoot!.querySelector('#daoMcpClient'));
+    assertNull(page.shadowRoot!.querySelector('#daoMcpStopControl'));
+  });
+
+  test('mcpShowsClientAndStopOnlyWhileConnected', function() {
+    sendMcpStatus({
+      enabled: true,
+      state: 'approval_requested',
+      clientName: 'Should not render',
+      clientPid: 99,
+      canStop: false,
+    });
+    assertNull(page.shadowRoot!.querySelector('#daoMcpClient'));
+    assertNull(page.shadowRoot!.querySelector('#daoMcpStopControl'));
+
+    sendMcpStatus({
+      enabled: true,
+      state: 'connected',
+      clientName: 'Codex',
+      clientPid: 12345,
+      canStop: true,
+    });
+
+    const status =
+        page.shadowRoot!.querySelector<HTMLElement>('#daoMcpStatusValue');
+    assertTrue(!!status);
+    assertEquals(
+        loadTimeData.getString('daoMcpStatusConnected'),
+        status.textContent.trim());
+    const client =
+        page.shadowRoot!.querySelector<HTMLElement>('#daoMcpClient');
+    assertTrue(!!client);
+    assertEquals(
+        loadTimeData.getStringF('daoMcpClientDetails', 'Codex', '12345'),
+        client.textContent.trim());
+    assertTrue(
+        !!page.shadowRoot!.querySelector('#daoMcpStopControl'));
+
+    sendMcpStatus({
+      enabled: true,
+      state: 'ready',
+      canStop: false,
+    });
+    assertNull(page.shadowRoot!.querySelector('#daoMcpClient'));
+    assertNull(page.shadowRoot!.querySelector('#daoMcpStopControl'));
+  });
+
+  test('mcpQuickSetupPreviewsAndCopiesSelectedClient', async function() {
+    assertNull(page.shadowRoot!.querySelector('#daoMcpQuickSetup'));
+
+    sendMcpStatus({
+      enabled: true,
+      state: 'ready',
+      canStop: false,
+    });
+    flush();
+    const codexCommand =
+        "codex mcp add dao -- '/Applications/Dao.app/Contents/Helpers/dao-mcp'";
+    mcpBrowserProxy.resolveSetupContent('codex', codexCommand);
+    await flushTasks();
+
+    const quickSetup =
+        page.shadowRoot!.querySelector<HTMLElement>('#daoMcpQuickSetup');
+    const selector =
+        page.shadowRoot!.querySelector<HTMLSelectElement>(
+            '#daoMcpClientSelector');
+    const preview =
+        page.shadowRoot!.querySelector<HTMLElement>(
+            '#daoMcpSetupPreview');
+    assertTrue(
+        !!page.shadowRoot!.querySelector('#daoMcpConnectionSection'));
+    const setupControls =
+        page.shadowRoot!.querySelector<HTMLElement>('#daoMcpSetupControls');
+    assertTrue(!!setupControls);
+    assertNull(page.shadowRoot!.querySelector('.dao-mcp-title'));
+    assertTrue(!!quickSetup);
+    assertTrue(!!selector);
+    assertTrue(!!preview);
+    assertEquals('codex', selector.value);
+    assertEquals(codexCommand, preview.textContent.trim());
+
+    selector.value = 'claude-code';
+    selector.dispatchEvent(new Event('change'));
+    const claudeCommand =
+        "claude mcp add --scope user dao -- '/Applications/Dao.app/Contents/Helpers/dao-mcp'";
+    mcpBrowserProxy.resolveSetupContent('claude-code', claudeCommand);
+    await flushTasks();
+    assertEquals(claudeCommand, preview.textContent.trim());
+
+    page.shadowRoot!
+        .querySelector<HTMLElement>('#daoMcpCopySetup')!.click();
+    assertEquals(
+        'claude-code',
+        await mcpBrowserProxy.whenCalled('copyDaoMcpSetupContent'));
+    mcpBrowserProxy.resolveCopyContent('claude-code', true);
+    await flushTasks();
+    const feedback = page.shadowRoot!.querySelector<HTMLElement>(
+        '#daoMcpSetupFeedback');
+    assertTrue(!!feedback);
+    assertEquals(
+        loadTimeData.getString('daoMcpInstallCommandCopied'),
+        feedback.textContent.trim());
+
+    selector.value = 'generic-mcp';
+    selector.dispatchEvent(new Event('change'));
+    const configuration =
+        '{\\n   "mcpServers": {\\n      "dao": {\\n         "command": "/Applications/Dao.app/Contents/Helpers/dao-mcp"\\n      }\\n   }\\n}\\n';
+    mcpBrowserProxy.resolveSetupContent('generic-mcp', configuration);
+    await flushTasks();
+    assertEquals(configuration.trim(), preview.textContent.trim());
+    assertEquals(
+        '{\\n   "mcpServers": {', preview.textContent.trim().slice(0, 20));
+
+    page.shadowRoot!
+        .querySelector<HTMLElement>('#daoMcpCopySetup')!.click();
+    assertEquals(
+        'generic-mcp',
+        await mcpBrowserProxy.whenCalled('copyDaoMcpSetupContent'));
+    mcpBrowserProxy.resolveCopyContent('generic-mcp', true);
+    await flushTasks();
+    assertEquals(
+        loadTimeData.getString('daoMcpConfigurationCopied'),
+        feedback.textContent.trim());
+
+    sendMcpStatus({
+      enabled: false,
+      state: 'disabled',
+      canStop: false,
+    });
+    flush();
+    assertNull(page.shadowRoot!.querySelector('#daoMcpQuickSetup'));
+  });
+
+  test('mcpQuickSetupIgnoresStalePreviewResponse', async function() {
+    sendMcpStatus({
+      enabled: true,
+      state: 'ready',
+      canStop: false,
+    });
+    flush();
+    const selector =
+        page.shadowRoot!.querySelector<HTMLSelectElement>(
+            '#daoMcpClientSelector')!;
+    selector.value = 'claude-code';
+    selector.dispatchEvent(new Event('change'));
+
+    mcpBrowserProxy.resolveSetupContent('claude-code', 'claude command');
+    await flushTasks();
+    mcpBrowserProxy.resolveSetupContent('codex', 'stale codex command');
+    await flushTasks();
+
+    const preview = page.shadowRoot!.querySelector<HTMLElement>(
+        '#daoMcpSetupPreview')!;
+    assertEquals('claude command', preview.textContent.trim());
+  });
+
+  test('mcpQuickSetupIgnoresCopyCompletionAfterDisableAndReenable',
+      async function() {
+    const codexCommand = 'codex command';
+    sendMcpStatus({
+      enabled: true,
+      state: 'ready',
+      canStop: false,
+    });
+    mcpBrowserProxy.resolveSetupContent('codex', codexCommand);
+    await flushTasks();
+
+    page.shadowRoot!
+        .querySelector<HTMLElement>('#daoMcpCopySetup')!.click();
+    assertEquals(
+        'codex', await mcpBrowserProxy.whenCalled('copyDaoMcpSetupContent'));
+
+    sendMcpStatus({
+      enabled: false,
+      state: 'disabled',
+      canStop: false,
+    });
+    sendMcpStatus({
+      enabled: true,
+      state: 'ready',
+      canStop: false,
+    });
+    mcpBrowserProxy.resolveCopyContent('codex', true);
+    mcpBrowserProxy.resolveSetupContent('codex', codexCommand);
+    await flushTasks();
+
+    const feedback = page.shadowRoot!.querySelector<HTMLElement>(
+        '#daoMcpSetupFeedback');
+    assertTrue(!!feedback);
+    assertEquals('', feedback.textContent.trim());
+  });
+
+  test('mcpQuickSetupDeclaresNarrowControlStacking', async function() {
+    sendMcpStatus({
+      enabled: true,
+      state: 'ready',
+      canStop: false,
+    });
+    mcpBrowserProxy.resolveSetupContent('codex', 'codex command');
+    await flushTasks();
+
+    const style = page.shadowRoot!.querySelector<HTMLStyleElement>('style');
+    assertTrue(!!style && !!style.sheet);
+    const narrowRule = Array.from(style.sheet.cssRules).find(
+        (rule): rule is CSSMediaRule =>
+            rule instanceof CSSMediaRule &&
+            rule.conditionText === '(max-width: 520px)');
+    assertTrue(!!narrowRule);
+    const controlsRule = Array.from(narrowRule.cssRules).find(
+        (rule): rule is CSSStyleRule =>
+            rule instanceof CSSStyleRule &&
+            rule.selectorText === '.dao-mcp-setup-controls');
+    assertTrue(!!controlsRule);
+    assertEquals('column', controlsRule.style.flexDirection);
+  });
+
+  test('mcpHasNoStandaloneCopyConfigButton', function() {
+    assertNull(page.shadowRoot!.querySelector('#daoMcpCopyConfig'));
+  });
+
+  test('mcpStopCallsHandlerWhenLeaseIsActive', async function() {
+    sendMcpStatus({
+      enabled: true,
+      state: 'connected',
+      clientName: 'Codex',
+      clientPid: 12345,
+      canStop: true,
+    });
+    const button =
+        page.shadowRoot!.querySelector<HTMLElement>('#daoMcpStopControl');
+    assertTrue(!!button);
+    button.click();
+
+    await mcpBrowserProxy.whenCalled('stopDaoMcpControl');
+  });
+
+  test('littleDaoToggleBindsToDaoPref', function() {
+    const toggle =
+        page.shadowRoot!.querySelector<SettingsToggleButtonElement>(
+            '#littleDaoEnabled');
+    assertTrue(!!toggle);
+    assertEquals('dao.little_dao_enabled', toggle.pref!.key);
+    assertTrue(toggle.checked);
+  });
+
+  test('enhancedCommandBarSuggestionsToggleBindsToDaoPref', function() {
+    const toggle =
+        page.shadowRoot!.querySelector<SettingsToggleButtonElement>(
+            '#enhancedCommandBarSuggestionsEnabled');
+    assertTrue(!!toggle);
+    assertEquals(
+        'dao.enhanced_command_bar_suggestions_enabled', toggle.pref!.key);
+    assertTrue(!toggle.checked);
+  });
+
+  test('askAiToggleBindsToDaoPref', function() {
+    const toggle =
+        page.shadowRoot!.querySelector<SettingsToggleButtonElement>(
+            '#askAiEnabled');
+    assertTrue(!!toggle);
+    assertEquals('dao.ask_ai_enabled', toggle.pref!.key);
+    assertTrue(toggle.checked);
+  });
+
+  test('enhancedPipToggleBindsToDaoPref', function() {
+    const toggle =
+        page.shadowRoot!.querySelector<SettingsToggleButtonElement>(
+            '#enhancedPipEnabled');
+    assertTrue(!!toggle);
+    assertEquals('dao.enhanced_pip_enabled', toggle.pref!.key);
+    assertTrue(toggle.checked);
+  });
+
+  test('staleTabExpirationInputSavesValidInteger', function() {
+    const input = page.shadowRoot!.querySelector<CrInputElement>(
+        '#staleTabExpirationHours');
+    assertTrue(!!input);
+    assertEquals('24', input.value);
+
+    input.value = '48';
+    input.dispatchEvent(new Event('input', {bubbles: true, composed: true}));
+    input.dispatchEvent(new Event('change', {bubbles: true, composed: true}));
+    flush();
+
+    assertFalse(input.invalid);
+    assertEquals(48, page.prefs['dao'].stale_tab_expiration_hours.value);
+  });
+
+  test('staleTabExpirationInputRejectsInvalidValue', function() {
+    const input = page.shadowRoot!.querySelector<CrInputElement>(
+        '#staleTabExpirationHours');
+    assertTrue(!!input);
+
+    input.value = '0';
+    input.dispatchEvent(new Event('input', {bubbles: true, composed: true}));
+    input.dispatchEvent(new Event('change', {bubbles: true, composed: true}));
+    flush();
+
+    assertTrue(input.invalid);
+    assertEquals(24, page.prefs['dao'].stale_tab_expiration_hours.value);
+  });
+
+  test('staleTabExpirationUnitIsInlineInputSuffix', function() {
+    const input = page.shadowRoot!.querySelector<CrInputElement>(
+        '#staleTabExpirationHours');
+    assertTrue(!!input);
+    assertTrue(!!input.querySelector('[slot="inline-suffix"]'));
+  });
+
+  test('enhancedPipPreviewShowsSelectedModeOnly', function() {
+    const preview = page.shadowRoot!.querySelector('#enhancedPipPreview');
+    assertTrue(!!preview);
+    assertTrue(!!preview.querySelector('#enhancedPipPreviewOn'));
+    assertTrue(!preview.querySelector('#enhancedPipPreviewOff'));
+    assertTrue(!!preview.querySelector('#enhancedPipPreviewSubtitles'));
+    assertTrue(!preview.querySelector('#enhancedPipPreviewOriginalWindow'));
+    assertTrue(!preview.querySelector('#enhancedPipPreviewOriginalVideo'));
+
+    page.set('prefs.dao.enhanced_pip_enabled.value', false);
+    flush();
+    assertTrue(!preview.querySelector('#enhancedPipPreviewOn'));
+    assertTrue(!preview.querySelector('#enhancedPipPreviewSubtitles'));
+    assertTrue(!!preview.querySelector('#enhancedPipPreviewOff'));
+    assertTrue(!!preview.querySelector('#enhancedPipPreviewOriginalWindow'));
+    assertTrue(!!preview.querySelector('#enhancedPipPreviewOriginalVideo'));
+  });
+
+  test('routeIsIndependentDaoSection', function() {
+    assertEquals('/dao', routes.DAO.path);
+    assertEquals('dao', routes.DAO.section);
+    assertEquals('daoPageTitle', routes.DAO.title);
+  });
+
+  test('navigatesToDaoRoute', function() {
+    Router.getInstance().navigateTo(routes.DAO);
+    assertEquals(routes.DAO, Router.getInstance().getCurrentRoute());
+  });
+});
