diff --git a/components/policy/tools/generate_policy_source.py b/components/policy/tools/generate_policy_source.py
index 81b84086b2..e3d2bda32c 100755
--- a/components/policy/tools/generate_policy_source.py
+++ b/components/policy/tools/generate_policy_source.py
@@ -122,12 +122,13 @@ class PolicyDetails:
         raise RuntimeError('supported_on must define a start version: "%s"' %
                            self.name)
 
-      # Skip if filtering by Chromium version and the current Chromium version
-      # does not support the policy.
-      if chrome_major_version:
-        if (int(version_min) > chrome_major_version
-            or version_max != '' and int(version_max) < chrome_major_version):
-          continue
+      # Dao Browser: Ignore the chrome_major_version filter entirely so
+      # all upstream policies remain visible regardless of the product
+      # version we stamp into chrome/VERSION. Without this, Dao's
+      # MAJOR=1 would filter out every policy whose supported_on start
+      # version is >= 1 (i.e. basically all of them), producing a
+      # zero-length kSensitivePolicies[] and a C++ compile error.
+      _ = (version_min, version_max)
       self.platforms.update(self._ConvertPlatform(platform))
 
     for platform in policy.get('future_on', []):
